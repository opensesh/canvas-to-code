# brain/home — iter-01 baseline (fixture)

Imported via the `paper-design` skill (synthetic — this is a test fixture).

- Captured: 2026-05-19
- Source: live page at `http://127.0.0.1:3000/brain`
- Variation intent: baseline (no design variation yet).

The bridge's Gate 1 should snapshot this iter into `.canvas-to-code/state/brain/source-snapshot/`. Gate 5a should copy `jsx/brain-home.tsx` straight to `/tmp/brain-template.tsx` without running the HTML extractor.
